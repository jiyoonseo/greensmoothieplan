/**
 * Created by junes on 1/17/2016.
 */


var express = require('express');
var router = express.Router();

var greenSmoothiePlan = 'Green Smoothie Plan';
/*

router.get('/:sub', function(req, res, next){
    //console.log("SUB= " + (req.params.sub));
    var sub = req.params.sub;
    res.render('layout' , { title: 'any:'+ sub });
});

router.get('/partial/:sub', function(req, res, next){
    //console.log("SUB= " + (req.params.sub));
    var sub = req.params.sub;
    res.render('partial/' + sub , { title: 'any:'+ sub });
});
*/


module.exports = router;